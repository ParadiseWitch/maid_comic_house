import type { FC } from 'react'
import PageTemplate from '/@/components/page-template'

const discover: FC = (_props) => {
  return (
    <PageTemplate>
    </PageTemplate>
  )
}

export default discover
